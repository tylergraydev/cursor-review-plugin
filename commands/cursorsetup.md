# /cursor:setup

Verify that the Cursor Agent CLI is installed and working correctly.

## Instructions

Run the setup check script:
```bash
bash <PLUGIN_DIR>/skills/cursor-review/scripts/cursor-setup.sh
```

If the setup check fails, guide the user through installation.
